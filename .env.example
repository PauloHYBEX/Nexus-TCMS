# ===========================================
# CONFIGURAÇÕES DO SUPABASE
# ===========================================
# Copie este arquivo para .env e preencha com suas credenciais reais

# URL do seu projeto Supabase (obrigatório)
VITE_SUPABASE_URL=https://seu-projeto.supabase.co

# Chave pública (anon) para o frontend (obrigatório)
VITE_SUPABASE_ANON_KEY=sua_anon_key_aqui

# ===========================================
# CONFIGURAÇÕES DO SISTEMA
# ===========================================

# Modo single tenant (true) ou multi-tenant (false)
# true = uma única organização, false = múltiplas organizações
VITE_SINGLE_TENANT=false

# URL do Studio externo (apps/ai)
# Ex.: http://localhost:3000 ou a URL de produção
VITE_STUDIO_URL=http://localhost:3000

# ===========================================
# EDGE FUNCTIONS (Opcional)
# ===========================================
# Estas variáveis são necessárias apenas se você for usar Edge Functions
# como convite de usuários, remoção de usuários, etc.

# Service Role Key do Supabase (cuidado! privilégios administrativos)
SUPABASE_SERVICE_ROLE_KEY=sua_service_role_key_aqui

# URL de redirecionamento para convites (para Edge Functions)
INVITE_REDIRECT_URL=http://localhost:8080

# ===========================================
# DESENVOLVIMENTO
# ===========================================

# Porta do servidor de desenvolvimento (padrão: 8080)
# PORT=8080

# Modo de desenvolvimento
# NODE_ENV=development
